#ifndef CONTROLLERS_TCP_PROTOCOL_GPSPACKET_GPSPACKET_H
#define CONTROLLERS_TCP_PROTOCOL_GPSPACKET_GPSPACKET_H

// GpsPacket 模块头文件

namespace PetRobot {

class GpsPacket {
public:
    GpsPacket();
    ~GpsPacket();

    // TODO: 添加公共接口

private:
    // TODO: 添加私有成员
};

} // namespace PetRobot

#endif // CONTROLLERS_TCP_PROTOCOL_GPSPACKET_GPSPACKET_H
