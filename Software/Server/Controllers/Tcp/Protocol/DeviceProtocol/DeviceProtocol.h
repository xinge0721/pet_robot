#ifndef CONTROLLERS_TCP_PROTOCOL_DEVICEPROTOCOL_DEVICEPROTOCOL_H
#define CONTROLLERS_TCP_PROTOCOL_DEVICEPROTOCOL_DEVICEPROTOCOL_H

// DeviceProtocol 模块头文件

namespace PetRobot {

class DeviceProtocol {
public:
    DeviceProtocol();
    ~DeviceProtocol();

    // TODO: 添加公共接口

private:
    // TODO: 添加私有成员
};

} // namespace PetRobot

#endif // CONTROLLERS_TCP_PROTOCOL_DEVICEPROTOCOL_DEVICEPROTOCOL_H
